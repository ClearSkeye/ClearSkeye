# Living Docs

This folder tracks the current minimal baseline.

## Current state

- Single route in `app/page.tsx`.
- Full-page black render with no UI features.
- No API routes, no environment variables, and no external integrations.
