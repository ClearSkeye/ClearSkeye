# Frontend Architecture

## Current UI

The frontend intentionally renders a single featureless page.

- `app/page.tsx` returns one `<main>` element.
- `app/globals.css` sets a full black viewport.
- No components, interactivity, copy, or visual sections.
